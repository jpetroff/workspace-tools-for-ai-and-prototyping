# Mesa packages for software-rendered Wayland workspaces.
RUN apt-get update && \
    apt-get install -y --no-install-recommends \
        libegl-mesa0 \
        libglx-mesa0 \
        mesa-utils \
        wayland-utils
