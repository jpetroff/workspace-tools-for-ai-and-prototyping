#!/usr/bin/env bash

set -u

while true; do
    printf '%s  |  %s  |  %s\n' \
        "${WORKSPACE_PROFILE_NAME:-workspace}" \
        "${WORKSPACE_RENDER_MODE:-unknown}" \
        "$(date '+%Y-%m-%d %H:%M:%S')"
    sleep 1
done

